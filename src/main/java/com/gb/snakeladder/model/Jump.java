package com.gb.snakeladder.model;

import lombok.Data;

/**
 * It can replace snakes and ladders as individual classes because they have same params
 */
@Data
public class Jump {

    private int start;
    private int end;
}
